log_format routing '$time_iso8601 $remote_addr "$request" → $upstream_service '
                   'status=$status upstream_addr=$upstream_addr '
                   'request_time=${request_time}s';

access_log /dev/stdout routing;
error_log  /dev/stderr warn;
